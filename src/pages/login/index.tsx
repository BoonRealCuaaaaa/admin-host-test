import Login from "@src/components/login";

const LoginPage = () => {
   return (
      <div className="h-screen w-full flex items-center justify-center overflow-hidden p-0 relative">
         <Login />
      </div>
   );
};

export default LoginPage;
